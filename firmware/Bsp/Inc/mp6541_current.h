/**
 * @file    mp6541_current.h
 * @brief   [已封存] MP6541 内置电流检测放大器常数
 *
 * ═══════════════════════════════════════════════════════════════════
 *  封存说明 —— 2026-07-26
 * ═══════════════════════════════════════════════════════════════════
 *
 *  本文件的常数是为 Phase 1 电流环准备的，但在调试中发现小电机
 *  空载/轻载下 MP6541 SOx 信噪比不足以做可靠电流反馈，已放弃。
 *
 *  保留此文件的原因：
 *    1. 完整的公式推导可参考（重新推导时节省时间）
 *    2. 硬件升级后可复用（更换大电机/更高电压/外部传感器）
 *
 *  当前状态：代码中无任何模块引用本文件的宏。
 *
 *  恢复使用条件：
 *    - 更换大功率电机（电流 > 1A）
 *    - 或改用外部电流传感器
 *    见 memory/current-sensing-retrospective.md
 *
 * ═══════════════════════════════════════════════════════════════════
 *
 * SPDX-License-Identifier: MIT
 * Copyright (c) 2024 The FOC Firmware Contributors
 *
 * ═══════════════════════════════════════════════════════════════════
 *  重要声明
 * ═══════════════════════════════════════════════════════════════════
 *
 *  本文件的常数完全基于以下硬件组合推导：
 *    ① MP6541 三相 BLDC 驱动器（内置电流检测放大器）
 *    ② PicoFOC 驱动板上的 5.1K+5.1K 电阻分压偏置网络
 *    ③ STM32F103C8T6 ADC1（12 位，3.3V 参考电压）
 *
 *  ── 换驱动芯片、换偏置电阻、换 MCU 后必须重新推导 ──
 *
 * ═══════════════════════════════════════════════════════════════════
 *  硬件电路与公式推导
 * ═══════════════════════════════════════════════════════════════════
 *
 *  【MP6541 电流检测架构】
 *
 *  MP6541 内部集成了三相电流检测放大器（Continuous-time 架构），
 *  三个 SOx 引脚（SOA/SOB/SOC）连续输出与瞬时负载电流成比例的检测电流。
 *  这是连续时间输出（非采样保持），在整个 PWM 周期都有效。
 *
 *  【PicoFOC 板的 SOx 偏置网络】
 *
 *     3.3V
 *       │
 *     5.1KΩ
 *       │
 *       +───→ SOx pin (PA0/PA1/PA2  → ADC1_IN0/1/2)
 *       │
 *     5.1KΩ
 *       │
 *      GND
 *
 *  这是电阻分压偏置网络，提供一个 1.65V 的直流偏置点。
 *
 *  【戴维南等效】
 *
 *    开路电压: Vth = 3.3V × 5.1K/(5.1K+5.1K) = 1.65V
 *    等效内阻: Rth = 5.1K || 5.1K = 2.55KΩ
 *
 *  【MP6541 增益（来自 datasheet 典型值）】
 *
 *    Phase A: Gain_A = 1/11600 A/A  (检测电流 / 负载电流)
 *    Phase B: Gain_B = 1/11500 A/A
 *    Phase C: Gain_C = 1/11000 A/A
 *
 *  【检测电流流过 Rth 产生的电压变化】
 *
 *    ΔV_per_A = Gain × Rth
 *    SOA: ΔV = (1/11600) × 2550 = 0.2198 V/A
 *    SOB: ΔV = (1/11500) × 2550 = 0.2217 V/A
 *    SOC: ΔV = (1/11000) × 2550 = 0.2318 V/A
 *
 *  【ADC 换算（以 Phase C 增益为基准，统一系数）】
 *
 *    ADC 分辨率: 12 位, 0~4095, 参考电压 3.3V
 *    LSB = 3.3V / 4096 = 0.0008057 V/LSB
 *
 *    每安培对应的 LSB 数（Phase C）:
 *    LSB_per_A = 0.2318 / 0.0008057 ≈ 287.7 LSB/A
 *
 *    每 LSB 对应的安培数（inv_scale）:
 *    inv_scale = 1 / 287.7 = 0.003476 A/LSB
 *
 *    统一公式（基于 Phase C 增益）:
 *    inv_scale = (Vref / resolution) × (11000 / 2550)
 *              = (3.3 / 4096) × (11000 / 2550)
 *              = 0.003476 A/LSB
 *
 *  【零电流偏移】
 *
 *    理想情况: I_load = 0A 时 V_SOx = 1.65V, ADC 读数 ≈ 2048
 *    实际偏差（电阻精度 ±5%、MP6541 输入偏置）: ±5~50 LSB
 *    需要通过零电流校准（ADC_CalibrateOffset）测量实际偏移值存储到 Flash。
 *    Phase 1 先用 2048 作为偏移值（零电流时读数约 0A）。
 *
 *  【为什么使用 Phase C 独立系数？】
 *
 *    Phase 1 统一使用 C 相系数 0.003476 A/LSB。
 *    如果 Phase 2 验证中发现三相幅值不平衡 >5%，再改用三相独立系数：
 *      inv_scale_a = (3.3/4096) × (11600/2550) = 0.003288 A/LSB
 *      inv_scale_b = (3.3/4096) × (11500/2550) = 0.003328 A/LSB
 *      inv_scale_c = (3.3/4096) × (11000/2550) = 0.003476 A/LSB
 *
 *  【ADC 精度评估】
 *
 *    分辨率: 3.47 mA/LSB
 *    有效位数（ENOB）: ~10.2 bit（12 bit ADC 典型值）
 *    噪声底: ~10~20 mA（PWM 开关噪声 + ADC 量化噪声）
 *    结论: 对 FOC 电流环控制完全够用。
 */

#ifndef __MP6541_CURRENT_H__
#define __MP6541_CURRENT_H__

/**
 * @brief 电流换算系数 (A/LSB)
 *
 * 用法: I_load(A) = (ADC_raw - offset) × MP6541_CURRENT_INV_SCALE
 *
 * 推导: (3.3V / 4096) × (11000 / 2550) = 0.003476
 *       基于 MP6541 Phase C 增益 1/11000 和 Rth=2.55KΩ
 *
 * @note Phase 1 三相使用统一系数，Phase 2 按需启用三相独立系数
 */
#define MP6541_CURRENT_INV_SCALE   (0.003476f)

/**
 * @brief 零电流理论 ADC 读数（1.65V 偏置）
 *
 * 3.3V 参考电压、12 位 ADC: 2048 = 4096 × 1.65V / 3.3V
 * Phase 1 硬编码此值做偏移修正，Phase 2 改为 Flash 存储的校准值
 */
#define MP6541_CURRENT_OFFSET_IDEAL  (2048)

#endif /* __MP6541_CURRENT_H__ */
